---
all: patch
---

# Prevent a login-screen flash during app launch by routing through a startup loading screen while auth restoration completes, then navigating directly to home (authenticated) or login (unauthenticated).


